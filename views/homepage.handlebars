
<!-- Loop over each code submission in the code-submissions array -->
{{#each submissions as |submission| }}
<div class="columns-2 mb-4 submission animatedPopIn">
  <div>
    <h2>
      <a href="/submission/{{submission.id}}">{{submission.name}}</a>
    </h2>
    <p>Created by {{submission.user.name}} on {{format_date submission.date_created}}</p>
  </div>
  <div>
    <p>
      {{submission.description}}
    </p>
  </div>
</div>
{{/each}}
